package ch07.sec06.package02;

import ch07.sec06.package01.A;

public class C {
    // 메서드 선언
    public void method(){
//        A a = new A();
//        a.field = "value";  // A 클래스의 생성자 A, 필드 field가 protected -> 접근 불가(다른 패키지)
//        a.method();    // A 클래스의 메소드 method가 protected -> 접근 불가(다른 패키지)
    }

}
